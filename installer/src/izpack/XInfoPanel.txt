Contratulations, ControlTier is installed

You may now visit Workbench at:

    http://$INSTALL_TOMCAT_HOSTNAME:$INSTALL_TOMCAT_PORT/itnav

Jobcenter is available at:

    http://$INSTALL_TOMCAT_HOSTNAME:$INSTALL_JOBCENTER_PORT/jobcenter

Reportcenter is available at:

    http://$INSTALL_TOMCAT_HOSTNAME:$INSTALL_REPORTCENTER_PORT/reportcenter

The software was installed in:

    $INSTALL_PATH

where you may find additional information and documentation.
